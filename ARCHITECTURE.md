# hypecapabilities — Architecture

## Overview
Provides a roles and capabilities API for Elgg, allowing plugins to define named capabilities and assign them to user roles for access-control decisions.

## Seeding
No seeder required. This plugin owns no entity types, subtypes, or persistent relationship schemas that would need synthetic data for development or testing. All data it interacts with belongs to other plugins' entities.
